services:
  realtime-gateway:
    build:
      context: ..
      dockerfile: services/realtime-gateway/Dockerfile
    env_file:
      - ../.env
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
