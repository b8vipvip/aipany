# Server-only secrets. Never commit real credentials.
AI_REALTIME_API_KEY=
DATABASE_URL=
REDIS_URL=
JWT_SECRET=

# Public client configuration may be added with an explicit PUBLIC_ prefix.
PUBLIC_API_BASE_URL=http://localhost:3000
