export const ADMIN_CONFIG_PAGE = String.raw`<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Aipany 服务配置</title>
<style>
:root{font-family:Inter,"PingFang SC","Microsoft YaHei",sans-serif;color:#172033;background:#f5f7fb;--primary:#4055d8;--primary-soft:#eef1ff;--border:#e4e7ec;--muted:#667085;--success:#067647;--danger:#b42318}*{box-sizing:border-box}body{margin:0;background:#f5f7fb}button,input,select,textarea{font:inherit}.hidden{display:none!important}.login-wrap{min-height:100vh;display:grid;place-items:center;padding:24px}.login-card{width:min(480px,100%);background:#fff;border:1px solid var(--border);border-radius:20px;padding:30px;box-shadow:0 24px 60px rgba(16,24,40,.08)}.login-card h1{margin:0 0 8px;font-size:28px}.login-card p{color:var(--muted);margin:0 0 24px}.shell{min-height:100vh;display:grid;grid-template-columns:250px 1fr}.sidebar{background:#111827;color:#d0d5dd;padding:22px 16px;position:sticky;top:0;height:100vh}.brand{padding:8px 10px 24px}.brand strong{display:block;color:#fff;font-size:20px}.brand span{font-size:12px;color:#98a2b3}.nav{display:flex;flex-direction:column;gap:5px}.nav a{color:#d0d5dd;text-decoration:none;padding:11px 12px;border-radius:10px;font-size:14px}.nav a:hover,.nav a.active{background:#26314d;color:#fff}.sidebar-bottom{position:absolute;left:16px;right:16px;bottom:20px}.content{min-width:0}.topbar{height:72px;background:rgba(255,255,255,.92);backdrop-filter:blur(12px);border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;padding:0 28px;position:sticky;top:0;z-index:5}.topbar h1{font-size:20px;margin:0}.top-actions{display:flex;gap:8px}.page-wrap{max-width:1180px;margin:0 auto;padding:28px}.page{display:none}.page.active{display:block}.page-head{margin-bottom:22px}.page-head h2{font-size:27px;margin:0 0 7px}.page-head p{margin:0;color:var(--muted)}.card{background:#fff;border:1px solid var(--border);border-radius:16px;padding:22px;margin-bottom:18px;box-shadow:0 6px 22px rgba(16,24,40,.035)}.card h3{margin:0 0 16px}.grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px}.grid4{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px}.field{display:flex;flex-direction:column;gap:7px}.field.full{grid-column:1/-1}label{font-weight:650;font-size:14px}input,select,textarea{width:100%;border:1px solid #d0d5dd;border-radius:10px;padding:11px 12px;background:#fff;color:#172033}textarea{resize:vertical;min-height:120px;line-height:1.55}input:focus,select:focus,textarea:focus{outline:2px solid #b8c0ff;border-color:#6172f3}.hint{font-size:12px;color:var(--muted);line-height:1.55}.btn{border:0;border-radius:10px;padding:10px 15px;font-weight:700;cursor:pointer}.btn:disabled{opacity:.55;cursor:not-allowed}.primary{background:var(--primary);color:#fff}.secondary{background:var(--primary-soft);color:#3448d8}.ghost{background:#f2f4f7;color:#344054}.danger{background:#fff1f0;color:var(--danger)}.small{padding:7px 10px;font-size:12px}.actions{display:flex;gap:9px;flex-wrap:wrap}.status{padding:11px 14px;border-radius:10px;background:#f2f4f7;color:#475467;font-size:13px;white-space:pre-wrap}.status.ok{background:#ecfdf3;color:var(--success)}.status.bad{background:#fef3f2;color:var(--danger)}.global-status{margin:0 28px 0;border-radius:0 0 12px 12px}.badge{display:inline-flex;align-items:center;padding:3px 8px;border-radius:999px;font-size:11px;background:#eef2ff;color:#3448d8}.badge.good{background:#ecfdf3;color:#067647}.badge.bad{background:#fef3f2;color:#b42318}.overview-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:14px}.metric{background:#fff;border:1px solid var(--border);border-radius:15px;padding:18px}.metric small{display:block;color:var(--muted);margin-bottom:8px}.metric strong{font-size:18px}.provider-card{border:1px solid #dfe3eb;border-radius:15px;padding:18px;margin-top:14px;background:#fcfcfd}.provider-head{display:flex;justify-content:space-between;gap:14px;align-items:flex-start;margin-bottom:14px}.provider-title{display:flex;align-items:center;gap:10px}.provider-title input[type=checkbox]{width:auto}.provider-title h3{margin:0;font-size:17px}.provider-models{margin-top:16px}.toolbar{display:flex;justify-content:space-between;align-items:center;gap:12px;margin-bottom:14px}.section-note{padding:12px 14px;border-radius:10px;background:#f8f9fc;color:#475467;font-size:13px;line-height:1.55;margin-bottom:14px}.table-wrap{overflow:auto;margin-top:12px}table{width:100%;border-collapse:collapse;font-size:12px}th,td{text-align:left;padding:9px 10px;border-bottom:1px solid #eaecf0;white-space:nowrap}th{color:#475467;background:#f9fafb}.test-summary{margin-top:12px}.codebox{font-family:ui-monospace,SFMono-Regular,Consolas,monospace;background:#101828;color:#e4e7ec;padding:14px;border-radius:12px;white-space:pre-wrap;line-height:1.55;max-height:380px;overflow:auto}.file-row{display:flex;gap:10px;align-items:center}.file-row input[type=file]{padding:8px}.route-card{cursor:pointer}.route-card:hover{border-color:#b8c0ff}.mobile-nav{display:none}.divider{height:1px;background:var(--border);margin:18px 0}@media(max-width:980px){.shell{grid-template-columns:1fr}.sidebar{display:none}.mobile-nav{display:block}.overview-grid,.grid4{grid-template-columns:repeat(2,minmax(0,1fr))}.topbar{padding:0 18px}.page-wrap{padding:20px}.global-status{margin:0 18px}}@media(max-width:700px){.grid,.grid4,.overview-grid{grid-template-columns:1fr}.top-actions .save-action{display:none}.provider-head,.toolbar{display:block}.provider-head .actions,.toolbar .actions{margin-top:10px}.page-wrap{padding:16px}.file-row{display:block}.file-row>*{margin-bottom:8px}}
</style>
</head>
<body>
<section id="login" class="login-wrap">
  <div class="login-card">
    <h1>Aipany 服务配置</h1>
    <p>输入服务器管理 Token。Token 仅保存在当前浏览器会话中。</p>
    <div class="field"><label>管理 Token</label><input id="adminToken" type="password" autocomplete="current-password" /></div>
    <div class="actions" style="margin-top:16px"><button class="btn primary" id="loginBtn">进入控制台</button></div>
    <div id="loginStatus" class="status" style="margin-top:14px">等待登录</div>
  </div>
</section>

<div id="app" class="shell hidden">
  <aside class="sidebar">
    <div class="brand"><strong>Aipany</strong><span>Service Console</span></div>
    <nav class="nav" id="nav">
      <a href="/admin/config" data-route="overview">总览</a>
      <a href="/admin/config/dashscope" data-route="dashscope">DashScope ASR / TTS</a>
      <a href="/admin/config/omni" data-route="omni">Qwen Omni</a>
      <a href="/admin/config/llm" data-route="llm">文本 LLM</a>
      <a href="/admin/config/remote" data-route="remote">Remote GPU</a>
      <a href="/admin/config/diagnostics" data-route="diagnostics">诊断测试</a>
      <a href="/admin/config/backup" data-route="backup">配置备份</a>
    </nav>
    <div class="sidebar-bottom"><button class="btn ghost" style="width:100%" id="logoutBtn">退出登录</button></div>
  </aside>

  <main class="content">
    <header class="topbar">
      <div><select id="mobileNav" class="mobile-nav"><option value="overview">总览</option><option value="dashscope">DashScope ASR / TTS</option><option value="omni">Qwen Omni</option><option value="llm">文本 LLM</option><option value="remote">Remote GPU</option><option value="diagnostics">诊断测试</option><option value="backup">配置备份</option></select><h1 id="pageTitle">总览</h1></div>
      <div class="top-actions"><button class="btn ghost save-action" id="reloadBtn">重新读取</button><button class="btn primary save-action" id="saveBtn">保存配置</button></div>
    </header>
    <div id="globalStatus" class="status global-status">尚未操作</div>
    <div class="page-wrap">
      <section class="page" data-page="overview">
        <div class="page-head"><h2>服务总览</h2><p>查看当前运行时配置状态，并进入各独立配置页面。</p></div>
        <div class="overview-grid">
          <div class="metric"><small>DashScope</small><strong id="overviewDash">检查中</strong></div>
          <div class="metric"><small>Qwen Omni</small><strong id="overviewOmni">检查中</strong></div>
          <div class="metric"><small>LLM 中转站</small><strong id="overviewLlm">检查中</strong></div>
          <div class="metric"><small>Remote GPU</small><strong id="overviewRemote">检查中</strong></div>
        </div>
        <div class="card" style="margin-top:18px"><h3>快速入口</h3><div class="grid"><button class="btn secondary route-card" data-go="llm">配置文本 LLM 与自动测速</button><button class="btn secondary route-card" data-go="diagnostics">运行完整 E2E 语音测试</button><button class="btn secondary route-card" data-go="backup">导出 / 导入加密配置</button><button class="btn secondary route-card" data-go="dashscope">配置 DashScope ASR / TTS</button></div></div>
      </section>

      <section class="page" data-page="dashscope">
        <div class="page-head"><h2>DashScope ASR / TTS</h2><p>配置实时语音识别和实时语音合成。</p></div>
        <div class="card"><div class="grid">
          <div class="field full"><label>DashScope API Key <span id="dashscopeState" class="badge">未配置</span></label><input id="DASHSCOPE_API_KEY" type="password" placeholder="留空保留已保存的 Key" /></div>
          <div class="field"><label>Workspace ID</label><input id="DASHSCOPE_WORKSPACE_ID" /></div>
          <div class="field"><label>Qwen ASR Model</label><input id="QWEN_ASR_MODEL" /></div>
          <div class="field full"><label>ASR WebSocket Base URL</label><input id="DASHSCOPE_ASR_WS_BASE_URL" placeholder="留空使用默认地址" /></div>
          <div class="field full"><label>TTS WebSocket Base URL</label><input id="DASHSCOPE_TTS_WS_BASE_URL" placeholder="留空使用默认地址" /></div>
          <div class="field"><label>Qwen TTS Model</label><input id="QWEN_TTS_MODEL" /></div>
          <div class="field"><label>Qwen TTS Voice</label><input id="QWEN_TTS_VOICE" /></div>
          <div class="field"><label>Qwen TTS Language</label><input id="QWEN_TTS_LANGUAGE" /></div>
        </div></div>
      </section>

      <section class="page" data-page="omni">
        <div class="page-head"><h2>Qwen Omni Cloud Audio</h2><p>管理环境理解、音频事件与云端多人转写增强。</p></div>
        <div class="card"><div class="grid">
          <div class="field full"><label>Omni API Key <span id="omniState" class="badge">未配置</span></label><input id="QWEN_OMNI_API_KEY" type="password" placeholder="可留空复用 DashScope Key；已保存时留空不修改" /></div>
          <div class="field full"><label>Omni Base URL</label><input id="QWEN_OMNI_BASE_URL" placeholder="留空使用默认 compatible-mode 地址" /></div>
          <div class="field"><label>Omni Model</label><input id="QWEN_OMNI_MODEL" /></div>
          <div class="field"><label>Cloud Audio Intelligence</label><select id="CLOUD_AUDIO_INTELLIGENCE_ENABLED"><option value="true">开启</option><option value="false">关闭</option></select></div>
          <div class="field"><label>Environment Intelligence</label><select id="CLOUD_AUDIO_ENVIRONMENT_ENABLED"><option value="true">开启</option><option value="false">关闭</option></select></div>
          <div class="field"><label>Diarized Transcription</label><select id="CLOUD_AUDIO_DIARIZED_TRANSCRIPTION_ENABLED"><option value="true">开启</option><option value="false">关闭</option></select></div>
        </div></div>
      </section>

      <section class="page" data-page="llm">
        <div class="page-head"><h2>文本 LLM Provider Pool</h2><p>多中转站故障切换、自动模型发现、协议兼容测试与速度排序。</p></div>
        <div class="card">
          <h3>故障切换策略</h3>
          <div class="grid4">
            <div class="field"><label>默认首 Token 超时(ms)</label><input id="LLM_POOL_FIRST_TOKEN_TIMEOUT_MS" type="number" min="1000" max="120000" /></div>
            <div class="field"><label>默认总超时(ms)</label><input id="LLM_POOL_TOTAL_TIMEOUT_MS" type="number" min="3000" max="300000" /></div>
            <div class="field"><label>失败冷却(ms)</label><input id="LLM_POOL_COOLDOWN_MS" type="number" min="1000" max="600000" /></div>
            <div class="field"><label>单次最大尝试路由数</label><input id="LLM_POOL_MAX_ATTEMPTS" type="number" min="1" max="100" /></div>
          </div>
        </div>
        <div class="card">
          <div class="toolbar"><div><h3 style="margin:0 0 5px">中转站</h3><div class="hint">勾选后可批量测试。模型优先级和请求协议顺序由测速自动生成，中转站优先级仍可手动控制。</div></div><div class="actions"><button class="btn secondary" id="testSelectedProvidersBtn">测试勾选中转站</button><button class="btn primary" id="addProviderBtn">添加中转站</button></div></div>
          <div class="section-note">自动测试会先调用模型列表接口，再逐个验证 Responses API 与 Chat Completions。只有两种 Aipany 请求方式都通过的模型才会自动加入模型池，并按平均首 Token 延迟由快到慢排序。</div>
          <div id="llmProviders"></div>
        </div>
      </section>

      <section class="page" data-page="remote">
        <div class="page-head"><h2>Remote GPU / SepFormer</h2><p>配置远程语音分离与目标说话人提取 Worker。</p></div>
        <div class="card"><div class="grid">
          <div class="field"><label>Remote Separation</label><select id="REMOTE_SEPARATION_ENABLED"><option value="false">关闭</option><option value="true">开启</option></select></div>
          <div class="field"><label>触发策略</label><select id="REMOTE_SEPARATION_TRIGGER"><option value="overlap_or_multi_speaker">重叠或多人</option><option value="overlap_only">仅重叠</option><option value="always_owner_focus">Owner Focus 总是调用</option></select></div>
          <div class="field full"><label>Remote Base URL</label><input id="REMOTE_SEPARATION_BASE_URL" /></div>
          <div class="field full"><label>Remote Token <span id="remoteState" class="badge">未配置</span></label><input id="REMOTE_SEPARATION_TOKEN" type="password" placeholder="留空保留已保存的 Token" /></div>
          <div class="field"><label>Timeout (ms)</label><input id="REMOTE_SEPARATION_TIMEOUT_MS" type="number" min="1000" max="120000" /></div>
        </div></div>
      </section>

      <section class="page" data-page="diagnostics">
        <div class="page-head"><h2>诊断测试</h2><p>无需终端脚本，直接从控制台验证完整实时语音链路。</p></div>
        <div class="card"><h3>完整 E2E 语音测试</h3><div class="section-note">测试路径：Qwen TTS 生成测试语音 → Gateway → Qwen ASR → LLM Provider Pool → Qwen TTS → PCM 音频返回。测试使用独立 deployment-test 会话。</div><div class="actions"><button class="btn primary" id="runE2eBtn">运行完整 E2E 测试</button></div><div id="e2eResult" class="codebox" style="margin-top:16px">尚未运行</div></div>
      </section>

      <section class="page" data-page="backup">
        <div class="page-head"><h2>配置备份</h2><p>将运行时 API 配置和已保存的密钥加密导出到本地，或从备份文件恢复。</p></div>
        <div class="card"><h3>导出加密备份</h3><div class="grid"><div class="field full"><label>备份密码</label><input id="exportPassphrase" type="password" placeholder="至少 8 个字符；恢复时需要相同密码" /></div></div><div class="actions" style="margin-top:14px"><button class="btn primary" id="exportBtn">导出到本地</button></div><p class="hint">备份文件使用 scrypt + AES-256-GCM 加密，API Key 不会以明文写入下载文件。</p></div>
        <div class="card"><h3>导入加密备份</h3><div class="grid"><div class="field full"><label>选择备份文件</label><input id="importFile" type="file" accept="application/json,.json" /></div><div class="field full"><label>备份密码</label><input id="importPassphrase" type="password" /></div></div><div class="actions" style="margin-top:14px"><button class="btn primary" id="importBtn">导入并覆盖运行时配置</button></div><p class="hint">导入只覆盖运行时 AI Provider 配置；JWT、数据库密码、声纹加密密钥等启动级配置不在备份范围内。</p></div>
      </section>
    </div>
  </main>
</div>

<script>
const AUTO_MODEL_SENTINEL="__aipany_auto_discover__";
const secretKeys=["DASHSCOPE_API_KEY","QWEN_OMNI_API_KEY","REMOTE_SEPARATION_TOKEN"];
const fields=["DASHSCOPE_WORKSPACE_ID","DASHSCOPE_ASR_WS_BASE_URL","DASHSCOPE_TTS_WS_BASE_URL","QWEN_ASR_MODEL","QWEN_TTS_MODEL","QWEN_TTS_VOICE","QWEN_TTS_LANGUAGE","QWEN_OMNI_BASE_URL","QWEN_OMNI_MODEL","CLOUD_AUDIO_INTELLIGENCE_ENABLED","CLOUD_AUDIO_ENVIRONMENT_ENABLED","CLOUD_AUDIO_DIARIZED_TRANSCRIPTION_ENABLED","REMOTE_SEPARATION_ENABLED","REMOTE_SEPARATION_BASE_URL","REMOTE_SEPARATION_TIMEOUT_MS","REMOTE_SEPARATION_TRIGGER"];
const routes={overview:"/admin/config",dashscope:"/admin/config/dashscope",omni:"/admin/config/omni",llm:"/admin/config/llm",remote:"/admin/config/remote",diagnostics:"/admin/config/diagnostics",backup:"/admin/config/backup"};
const routeTitles={overview:"总览",dashscope:"DashScope ASR / TTS",omni:"Qwen Omni",llm:"文本 LLM",remote:"Remote GPU",diagnostics:"诊断测试",backup:"配置备份"};
let token=sessionStorage.getItem("aipanyAdminToken")||"";
let llmPool=defaultPool();
let relayResults={};
const $=id=>document.getElementById(id);
function auth(){return {Authorization:"Bearer "+token,"Content-Type":"application/json"}}
function setStatus(text,ok){const el=$("globalStatus");el.textContent=text;el.className="status global-status "+(ok===true?"ok":ok===false?"bad":"")}
function defaultPool(){return {providers:[],firstTokenTimeoutMs:12000,totalTimeoutMs:60000,cooldownMs:60000,maxAttempts:8}}
function defaultValue(key){const defaults={QWEN_ASR_MODEL:"qwen3-asr-flash-realtime",QWEN_TTS_MODEL:"qwen3-tts-instruct-flash-realtime",QWEN_TTS_VOICE:"Cherry",QWEN_TTS_LANGUAGE:"Chinese",QWEN_OMNI_MODEL:"qwen3.5-omni-flash",CLOUD_AUDIO_INTELLIGENCE_ENABLED:"true",CLOUD_AUDIO_ENVIRONMENT_ENABLED:"true",CLOUD_AUDIO_DIARIZED_TRANSCRIPTION_ENABLED:"true",REMOTE_SEPARATION_ENABLED:"false",REMOTE_SEPARATION_TIMEOUT_MS:"30000",REMOTE_SEPARATION_TRIGGER:"overlap_or_multi_speaker"};return defaults[key]||""}
function uid(prefix){return window.crypto&&crypto.randomUUID?prefix+"-"+crypto.randomUUID():prefix+"-"+Date.now()+"-"+Math.random().toString(16).slice(2)}
function num(value,fallback){const n=Number(value);return Number.isFinite(n)?Math.round(n):fallback}
function create(tag,className,text){const node=document.createElement(tag);if(className)node.className=className;if(text!==undefined)node.textContent=text;return node}
function makeInput(type,value,placeholder){const input=create("input");input.type=type||"text";input.value=value===undefined||value===null?"":String(value);if(placeholder)input.placeholder=placeholder;return input}
function makeSelect(options,value){const select=create("select");for(const item of options){const option=create("option");option.value=item[0];option.textContent=item[1];select.appendChild(option)}select.value=String(value);return select}
function addField(parent,labelText,control,full,hint){const box=create("div","field"+(full?" full":""));box.appendChild(create("label",null,labelText));box.appendChild(control);if(hint)box.appendChild(create("div","hint",hint));parent.appendChild(box);return box}
function normalizePool(pool){const source=pool&&typeof pool==="object"?pool:defaultPool();return {providers:Array.isArray(source.providers)?source.providers.map(p=>({id:p.id||uid("provider"),name:p.name||"新中转站",baseUrl:p.baseUrl||"",apiKey:"",apiKeyConfigured:Boolean(p.apiKeyConfigured),enabled:p.enabled!==false,priority:num(p.priority,100),firstTokenTimeoutMs:p.firstTokenTimeoutMs===undefined?undefined:num(p.firstTokenTimeoutMs,12000),totalTimeoutMs:p.totalTimeoutMs===undefined?undefined:num(p.totalTimeoutMs,60000),models:Array.isArray(p.models)?p.models.filter(m=>m.id!==AUTO_MODEL_SENTINEL).map(m=>({id:m.id||"",enabled:m.enabled!==false,priority:num(m.priority,100),protocols:Array.isArray(m.protocols)&&m.protocols.length?m.protocols:["responses","chat_completions"]})):[]})):[],firstTokenTimeoutMs:num(source.firstTokenTimeoutMs,12000),totalTimeoutMs:num(source.totalTimeoutMs,60000),cooldownMs:num(source.cooldownMs,60000),maxAttempts:num(source.maxAttempts,8)}}
function modelText(provider){return provider.models.filter(m=>m.id&&m.id!==AUTO_MODEL_SENTINEL).sort((a,b)=>a.priority-b.priority).map(m=>m.id).join("\n")}
function updateModelsFromText(provider,text){const ids=[...new Set(text.split(/[\n,]+/).map(x=>x.trim()).filter(Boolean))];const previous=new Map(provider.models.map(m=>[m.id,m]));provider.models=ids.map((id,index)=>{const old=previous.get(id);return {id:id,enabled:true,priority:(index+1)*10,protocols:old&&Array.isArray(old.protocols)&&old.protocols.length?old.protocols.slice():["responses","chat_completions"]}})}
function poolForSave(){llmPool.firstTokenTimeoutMs=num($("LLM_POOL_FIRST_TOKEN_TIMEOUT_MS").value,12000);llmPool.totalTimeoutMs=num($("LLM_POOL_TOTAL_TIMEOUT_MS").value,60000);llmPool.cooldownMs=num($("LLM_POOL_COOLDOWN_MS").value,60000);llmPool.maxAttempts=num($("LLM_POOL_MAX_ATTEMPTS").value,8);return {providers:llmPool.providers.map(p=>{const models=p.models.length?p.models.map((m,index)=>({id:m.id,enabled:m.enabled!==false,priority:(index+1)*10,protocols:m.protocols&&m.protocols.length?m.protocols.slice():["responses","chat_completions"]})):[{id:AUTO_MODEL_SENTINEL,enabled:false,priority:10000,protocols:["chat_completions"]}];const out={id:p.id,name:(p.name||"").trim(),baseUrl:(p.baseUrl||"").trim(),apiKey:(p.apiKey||"").trim(),enabled:Boolean(p.enabled),priority:num(p.priority,100),models:models};if(p.firstTokenTimeoutMs!==undefined)out.firstTokenTimeoutMs=num(p.firstTokenTimeoutMs,12000);if(p.totalTimeoutMs!==undefined)out.totalTimeoutMs=num(p.totalTimeoutMs,60000);return out}),firstTokenTimeoutMs:llmPool.firstTokenTimeoutMs,totalTimeoutMs:llmPool.totalTimeoutMs,cooldownMs:llmPool.cooldownMs,maxAttempts:llmPool.maxAttempts}}
function currentRoute(){const path=location.pathname.replace(/\/$/,"");for(const [key,value] of Object.entries(routes)){if(value===path||value+"/"===location.pathname)return key}return "overview"}
function navigate(route,push=true){const target=routes[route]||routes.overview;if(push&&location.pathname!==target)history.pushState({},"",target);document.querySelectorAll(".page").forEach(el=>el.classList.toggle("active",el.dataset.page===route));document.querySelectorAll("#nav a").forEach(el=>el.classList.toggle("active",el.dataset.route===route));$("pageTitle").textContent=routeTitles[route]||"Aipany 服务配置";$("mobileNav").value=route;const configPage=["dashscope","omni","llm","remote"].includes(route);document.querySelectorAll(".save-action").forEach(el=>el.classList.toggle("hidden",!configPage))}
async function loadConfig(){const r=await fetch("/admin/api/config",{headers:auth()});if(!r.ok)throw new Error(await r.text());const data=await r.json();for(const key of fields){const el=$(key);if(el)el.value=data.values[key]||defaultValue(key)}$("dashscopeState").textContent=data.secrets.DASHSCOPE_API_KEY&&data.secrets.DASHSCOPE_API_KEY.configured?"已配置":"未配置";$("dashscopeState").className="badge "+(data.secrets.DASHSCOPE_API_KEY&&data.secrets.DASHSCOPE_API_KEY.configured?"good":"");$("omniState").textContent=data.secrets.QWEN_OMNI_API_KEY&&data.secrets.QWEN_OMNI_API_KEY.configured?"已配置/或复用":"未单独配置";$("remoteState").textContent=data.secrets.REMOTE_SEPARATION_TOKEN&&data.secrets.REMOTE_SEPARATION_TOKEN.configured?"已配置":"未配置";llmPool=normalizePool(data.llmProviderPool);renderLlmPool();renderOverview(data);setStatus("配置已读取。保存路径："+data.path,true);return data}
function renderOverview(data){const dash=data.secrets.DASHSCOPE_API_KEY&&data.secrets.DASHSCOPE_API_KEY.configured;const omni=data.secrets.QWEN_OMNI_API_KEY&&data.secrets.QWEN_OMNI_API_KEY.configured;$("overviewDash").textContent=dash?"已配置":"未配置";$("overviewOmni").textContent=omni||dash?"可用":"未配置";$("overviewLlm").textContent=llmPool.providers.filter(p=>p.models.length>0).length+" 个中转站";$("overviewRemote").textContent=data.values.REMOTE_SEPARATION_ENABLED==="true"?"已开启":"未开启"}
async function save(silent){const body={llmProviderPool:poolForSave()};for(const key of fields)body[key]=$(key).value.trim();for(const key of secretKeys){const value=$(key).value.trim();if(value)body[key]=value}if(!silent)setStatus("正在保存配置…");const r=await fetch("/admin/api/config",{method:"PUT",headers:auth(),body:JSON.stringify(body)});if(!r.ok){const message=await r.text();if(!silent)setStatus("保存失败："+message,false);throw new Error(message)}for(const key of secretKeys)$(key).value="";await loadConfig();if(!silent)setStatus("保存成功。新建立的实时会话会立即使用最新配置。",true)}
function renderLlmPool(){$("LLM_POOL_FIRST_TOKEN_TIMEOUT_MS").value=String(llmPool.firstTokenTimeoutMs);$("LLM_POOL_TOTAL_TIMEOUT_MS").value=String(llmPool.totalTimeoutMs);$("LLM_POOL_COOLDOWN_MS").value=String(llmPool.cooldownMs);$("LLM_POOL_MAX_ATTEMPTS").value=String(llmPool.maxAttempts);const container=$("llmProviders");container.innerHTML="";if(!llmPool.providers.length){container.appendChild(create("div","section-note","尚未配置中转站。点击“添加中转站”后填写 Base URL 和 API Key，再运行自动测试。"));return}llmPool.providers.forEach((provider,index)=>container.appendChild(renderProvider(provider,index)))}
function renderProvider(provider,index){const card=create("div","provider-card");const head=create("div","provider-head");const titleWrap=create("div","provider-title");const check=makeInput("checkbox");check.checked=Boolean(provider.selected);check.onchange=()=>{provider.selected=check.checked};titleWrap.appendChild(check);titleWrap.appendChild(create("h3",null,(index+1)+". "+(provider.name||"未命名中转站")));titleWrap.appendChild(create("span","badge "+(provider.apiKeyConfigured?"good":""),provider.apiKeyConfigured?"Key 已保存":"Key 未保存"));head.appendChild(titleWrap);const headActions=create("div","actions");const testBtn=create("button","btn small secondary","测试此中转站");testBtn.type="button";testBtn.onclick=()=>runRelayTests([provider.id]);headActions.appendChild(testBtn);const remove=create("button","btn small danger","删除");remove.type="button";remove.onclick=()=>{if(confirm("确定删除这个中转站吗？")){llmPool.providers.splice(index,1);renderLlmPool()}};headActions.appendChild(remove);head.appendChild(headActions);card.appendChild(head);
const grid=create("div","grid");const name=makeInput("text",provider.name);name.oninput=()=>{provider.name=name.value};addField(grid,"名称",name,false);const enabled=makeSelect([["true","启用"],["false","停用"]],provider.enabled?"true":"false");enabled.onchange=()=>{provider.enabled=enabled.value==="true"};addField(grid,"状态",enabled,false);const url=makeInput("url",provider.baseUrl,"https://example.com/v1");url.oninput=()=>{provider.baseUrl=url.value};addField(grid,"Base URL",url,true);const key=makeInput("password","",provider.apiKeyConfigured?"留空保留已保存的 Key":"请输入 API Key");key.oninput=()=>{provider.apiKey=key.value};addField(grid,"API Key",key,true,"Provider ID 不变时，留空会保留服务器已保存的 Key。");const priority=makeInput("number",provider.priority);priority.min="0";priority.max="10000";priority.oninput=()=>{provider.priority=num(priority.value,100)};addField(grid,"中转站优先级",priority,false,"数字越小越优先；这是唯一需要手动设置的优先级。");const first=makeInput("number",provider.firstTokenTimeoutMs===undefined?"":provider.firstTokenTimeoutMs,"留空继承全局");first.min="1000";first.max="120000";first.oninput=()=>{provider.firstTokenTimeoutMs=first.value?num(first.value,llmPool.firstTokenTimeoutMs):undefined};addField(grid,"首 Token 超时(ms)",first,false);const total=makeInput("number",provider.totalTimeoutMs===undefined?"":provider.totalTimeoutMs,"留空继承全局");total.min="3000";total.max="300000";total.oninput=()=>{provider.totalTimeoutMs=total.value?num(total.value,llmPool.totalTimeoutMs):undefined};addField(grid,"总超时(ms)",total,false);card.appendChild(grid);
const modelsBox=create("div","provider-models");const textarea=create("textarea");textarea.value=modelText(provider);textarea.placeholder="自动测试后会按速度写入，例如：\ngpt-5.6-luna\ngpt-5.6-sol";textarea.oninput=()=>updateModelsFromText(provider,textarea.value);addField(modelsBox,"可用模型（顺序即自动优先级）",textarea,true,"每行或逗号分隔一个模型。自动测试只保留 Responses 与 Chat Completions 都通过的模型，并自动按首 Token 速度排序。协议顺序也由测速自动配置，无需手动选择。");card.appendChild(modelsBox);if(relayResults[provider.id])card.appendChild(renderBenchmark(relayResults[provider.id]));return card}
function renderBenchmark(result){const wrap=create("div","test-summary");const summary=create("div","status "+(result.ok?"ok":"bad"),result.ok?"测试完成：发现 "+result.discoveredModels+" 个模型，"+result.eligibleModels.length+" 个模型同时通过两种请求方式，总耗时 "+result.elapsedMs+"ms":"测试失败："+(result.error||"没有符合条件的模型"));wrap.appendChild(summary);if(Array.isArray(result.results)&&result.results.length){const tableWrap=create("div","table-wrap");const table=create("table");const head=create("thead");const hr=create("tr");["模型","状态","综合首 Token","Responses","Chat Completions","自动首选协议"].forEach(x=>hr.appendChild(create("th",null,x)));head.appendChild(hr);table.appendChild(head);const body=create("tbody");result.results.forEach(item=>{const row=create("tr");const protocolMap={};(item.protocols||[]).forEach(p=>protocolMap[p.protocol]=p);const responses=protocolMap.responses;const chat=protocolMap.chat_completions;const preferred=item.eligible?[responses,chat].filter(Boolean).sort((a,b)=>(a.firstTokenMs||999999)-(b.firstTokenMs||999999))[0]:null;[item.model,item.eligible?"可用":"未通过",item.scoreMs===undefined?"-":item.scoreMs+" ms",responses&&responses.success?responses.firstTokenMs+" ms":"失败",chat&&chat.success?chat.firstTokenMs+" ms":"失败",preferred?(preferred.protocol==="responses"?"Responses":"Chat"):"-"].forEach(x=>row.appendChild(create("td",null,String(x))));body.appendChild(row)});table.appendChild(body);tableWrap.appendChild(table);wrap.appendChild(tableWrap)}return wrap}
async function runRelayTests(providerIds){try{const ids=providerIds&&providerIds.length?providerIds:llmPool.providers.filter(p=>p.selected).map(p=>p.id);if(!ids.length)throw new Error("请先勾选至少一个中转站");setStatus("正在保存中转站配置并进行深度测试。模型较多时可能需要几分钟，请保持页面打开…");await save(true);const r=await fetch("/admin/api/config/relay-test",{method:"POST",headers:auth(),body:JSON.stringify({providerIds:ids})});const data=await r.json();if(!r.ok)throw new Error(data.message||JSON.stringify(data));for(const result of data.results||[])relayResults[result.providerId]=result;llmPool=normalizePool(data.config.llmProviderPool);renderLlmPool();renderOverview(data.config);const passed=(data.results||[]).filter(x=>x.ok).length;setStatus("中转站测试完成："+passed+" / "+ids.length+" 个中转站生成了可用双协议模型池。",passed>0)}catch(e){setStatus("中转站测试失败："+e.message,false)}}
async function runE2e(){const button=$("runE2eBtn");button.disabled=true;$("e2eResult").textContent="正在执行完整 E2E 测试，请稍候…";setStatus("正在测试 ASR → LLM Provider Pool → TTS…");try{const r=await fetch("/admin/api/config/e2e-test",{method:"POST",headers:auth(),body:"{}"});const data=await r.json();if(!r.ok)throw new Error(data.message||JSON.stringify(data));const lines=["PASS：完整实时语音链路测试通过","","测试输入 TTS："+data.inputTtsBytes+" bytes","ASR："+data.transcript,"LLM："+data.answerText,"返回 TTS："+data.responseAudioBytes+" bytes","","输入 TTS 耗时："+data.timings.inputTtsMs+" ms","会话 Ready："+data.timings.sessionReadyMs+" ms","ASR Final："+(data.timings.asrFinalMs===undefined?"-":data.timings.asrFinalMs+" ms"),"LLM 首 Token："+(data.timings.llmFirstTokenMs===undefined?"-":data.timings.llmFirstTokenMs+" ms"),"完整耗时："+data.timings.totalMs+" ms"];$("e2eResult").textContent=lines.join("\n");setStatus("E2E 完整链路测试通过。",true)}catch(e){$("e2eResult").textContent="FAIL："+e.message;setStatus("E2E 测试失败："+e.message,false)}finally{button.disabled=false}}
async function exportBackup(){try{const passphrase=$("exportPassphrase").value;if(passphrase.length<8)throw new Error("备份密码至少 8 个字符");setStatus("正在生成加密备份…");const r=await fetch("/admin/api/config/export",{method:"POST",headers:auth(),body:JSON.stringify({passphrase:passphrase})});const data=await r.json();if(!r.ok)throw new Error(data.message||JSON.stringify(data));const blob=new Blob([JSON.stringify(data.backup,null,2)],{type:"application/json"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=data.filename||"aipany-config-backup.json";document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url);setStatus("加密配置备份已导出到本地。",true)}catch(e){setStatus("导出失败："+e.message,false)}}
async function importBackup(){try{const file=$("importFile").files&&$("importFile").files[0];if(!file)throw new Error("请选择备份文件");const passphrase=$("importPassphrase").value;if(passphrase.length<8)throw new Error("请输入导出备份时使用的密码");if(!confirm("导入将覆盖当前运行时 API 配置，确定继续吗？"))return;setStatus("正在解密并导入配置…");const backup=JSON.parse(await file.text());const r=await fetch("/admin/api/config/import",{method:"POST",headers:auth(),body:JSON.stringify({passphrase:passphrase,backup:backup})});const data=await r.json();if(!r.ok)throw new Error(data.message||JSON.stringify(data));await loadConfig();setStatus("配置导入成功。新建立的实时会话会使用恢复后的配置。",true)}catch(e){setStatus("导入失败："+e.message,false)}}
async function login(){token=$("adminToken").value.trim();if(!token)return;sessionStorage.setItem("aipanyAdminToken",token);try{await loadConfig();$("login").classList.add("hidden");$("app").classList.remove("hidden");navigate(currentRoute(),false)}catch(e){$("loginStatus").textContent="登录失败："+e.message;sessionStorage.removeItem("aipanyAdminToken")}}
$("loginBtn").onclick=login;$("adminToken").addEventListener("keydown",e=>{if(e.key==="Enter")login()});$("saveBtn").onclick=()=>save(false).catch(()=>{});$("reloadBtn").onclick=()=>loadConfig().catch(e=>setStatus(e.message,false));$("logoutBtn").onclick=()=>{sessionStorage.removeItem("aipanyAdminToken");location.reload()};$("addProviderBtn").onclick=()=>{llmPool.providers.push({id:uid("provider"),name:"新中转站",baseUrl:"",apiKey:"",apiKeyConfigured:false,enabled:false,priority:(llmPool.providers.length+1)*100,models:[],selected:true});renderLlmPool()};$("testSelectedProvidersBtn").onclick=()=>runRelayTests();$("runE2eBtn").onclick=runE2e;$("exportBtn").onclick=exportBackup;$("importBtn").onclick=importBackup;$("mobileNav").onchange=()=>navigate($("mobileNav").value,true);document.querySelectorAll("#nav a").forEach(a=>a.addEventListener("click",e=>{e.preventDefault();navigate(a.dataset.route,true)}));document.querySelectorAll("[data-go]").forEach(el=>el.addEventListener("click",()=>navigate(el.dataset.go,true)));window.addEventListener("popstate",()=>navigate(currentRoute(),false));if(token){loadConfig().then(()=>{$("login").classList.add("hidden");$("app").classList.remove("hidden");navigate(currentRoute(),false)}).catch(()=>sessionStorage.removeItem("aipanyAdminToken"))}else navigate(currentRoute(),false);
</script>
</body></html>`;
