import Fastify from "fastify";import type { AdminApiConfig } from "./config.js";import { registerProviderRoutes } from "./modules/providers/provider-config.controller.js";import type { ProviderConfigService } from "./modules/providers/provider-config.service.js";
export function buildApp(config:Pick<AdminApiConfig,"ADMIN_API_TOKEN"|"NODE_ENV">, providers:ProviderConfigService){const app=Fastify({logger:true});app.get("/health",async()=>({ok:true,service:"admin-api"}));app.addHook("onRequest",async(req,reply)=>{if(!req.url.startsWith("/v1/admin")||!config.ADMIN_API_TOKEN)return;const auth=req.headers.authorization;if(auth!==`Bearer ${config.ADMIN_API_TOKEN}`)return reply.code(401).send({error:{code:"UNAUTHORIZED",message:"缺少或错误的 Admin Bearer Token"}});});registerProviderRoutes(app,providers);app.setErrorHandler((e,req,reply)=>{req.log.warn({message:e.message},"Admin API 请求失败");reply.code(400).send({error:{code:"BAD_REQUEST",message:e.message}})});return app;}
