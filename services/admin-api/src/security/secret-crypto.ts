import { createCipheriv, createDecipheriv, createHash, randomBytes } from "node:crypto";
export interface EncryptedSecret{ciphertext:string;iv:string;authTag:string}
function keyBytes(key:string):Buffer{const raw=Buffer.from(key,"base64"); if(raw.length===32)return raw; if(Buffer.byteLength(key)===32)return Buffer.from(key); return createHash("sha256").update(key).digest();}
export class SecretCrypto{constructor(private readonly key:string){} encrypt(plain:string):EncryptedSecret{const iv=randomBytes(12);const cipher=createCipheriv("aes-256-gcm",keyBytes(this.key),iv);const ciphertext=Buffer.concat([cipher.update(plain,"utf8"),cipher.final()]);return{ciphertext:ciphertext.toString("base64"),iv:iv.toString("base64"),authTag:cipher.getAuthTag().toString("base64")};} decrypt(s:EncryptedSecret):string{const d=createDecipheriv("aes-256-gcm",keyBytes(this.key),Buffer.from(s.iv,"base64"));d.setAuthTag(Buffer.from(s.authTag,"base64"));return Buffer.concat([d.update(Buffer.from(s.ciphertext,"base64")),d.final()]).toString("utf8");}}
export function maskSecret(secret?:string|null):string|null{if(!secret)return null;const tail=secret.slice(-4);const head=secret.startsWith("sk-")?"sk-":"";return `${head}****${tail}`;}
